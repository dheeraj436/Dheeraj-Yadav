apiVersion: v1
kind: Service

metadata:
  name: SpringSecurityAndJWT-Service

spec:
  type: NodePort

  selector:
    app: SpringSecurityAndJWT-app

  ports:
    - port: 80
      targetPort: 8090
      nodePort: 30080
